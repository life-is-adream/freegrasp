# PathB B1 消融实验设计（2026-09-21）

> 对象：当前**成功率最高方法 EGHC3_v3** 之上的 **pathB1（= `pathb_blk_arb_v1`）** 单变量消融。
> 目标：判定 B1 是否对 EGHC3_v3 有真实增益；若无，则作为**有效 null closure**归档。

---

## 0. 结论先行

1. **B1 消融其实已经跑过**（`PATHB_FULL_B1_blk_arb`，2026-08-08，n=900），但**那次 run 不是干净的消融**——至少 4 个变量同时变化，结论不可归因。
2. **已有数据给出的信号是"B1 净效应 ≈ 0"**：在可比性最好的 split-0 上，B1 与 EGHC3_v3 的 **RSR 完全相同（186/300 对 186/300）**，SSR 仅差 1 例（171 vs 172）。full900 看似 +1.33pp 的差距**全部来自 split 1/2**，而那部分两 run 的 git 基线不同、不可直接比。
3. **推荐路径**：先做 **Stage A（零成本、离线、不需要 GPU）** 的配对再分析把已有数据用足；只有 Stage A 显示 blocker_first 子集有机制信号时，才启动 **Stage B（付费重跑纯单变量臂）**。
4. ⚠️ **硬前置**：服务器 GPU 驱动当前**已损坏**（`nvidia-smi` 报无法与驱动通信），任何需要 SAM 的**重跑在驱动修复前都不可执行**。Stage A 不受此限。

---

## 1. 对象定义（已核实，非推测）

### 1.1 PathB 与 B1

来源：`experiments/pathb_stage0_apply.py`（2026-08-09）+ `utils/eghc_router.py:20-25`

| 变体 | 常量 | 含义 |
|---|---|---|
| **B1** | `pathb_blk_arb_v1` | **blocker_first 放宽仲裁**（relaxed arbitration） |
| B1+B3 | `pathb_blk_clut_v1` | B1 + 杂波/多候选触发 |
| B2 | `pathb_disamb_v1` | 消歧路由 prompt |

**B1 的机制**（`utils/eghc_adjudicator.py:331` 附近）：
在 `blocker_first` 路由下，允许在**候选池内**（目标候选 + 已声明障碍 + 当前计划 ID）以 `confidence >= PATHB_MIN_CONFIDENCE` 做替换；**绝不虚构第三个物体**；若仲裁结果 == 当前计划 ID 则 `pathb_keep` 拒绝替换。

> ⇒ B1 **只在 blocker_first 路径上可能生效**。这一点决定了"全样本主终点 + blocker_first 条件分析"的双层设计（见 §4.4）。

### 1.2 成功率最高的方法

**EGHC3_v3**（当前最高可部署方法）：
- split-0（n=300）：**RSR 186/300 = 62.00% / SSR 172/300 = 57.33%**（源：`data/output/ablation_matrix/EGHC3_v3/full_protocol_summary.json`）
- full900（n=900）：RSR 548/900 = 60.89% / SSR 497/900 = 55.22%（源：`paper_v7_20260731_unograsp_reframed/figure_assets/source_data/`，**属另一次 run**）

### 1.3 已跑过的 B1 结果

源：`data/output/ablation_matrix/PATHB_FULL_B1_blk_arb/`（splits 0/1/2，n=900）

| 口径 | RSR | SSR |
|---|---|---|
| **split-0（n=300）** | **186/300 = 62.00%** | **171/300 = 57.00%** |
| full900（n=900） | 560/900 = 62.22% | 514/900 = 57.11% |

**split-0 关键对比（可比性最好）**

| 臂 | RSR | SSR |
|---|---|---|
| EGHC3_v3（基线） | 186/300 = 62.00% | 172/300 = 57.33% |
| B1 | 186/300 = 62.00% | 171/300 = 57.00% |
| **差** | **0（完全相同）** | **−1 例（−0.33pp）** |

---

## 2. 已有 B1 run 为什么不是干净消融（缺陷清单）

| # | 混淆项 | EGHC3_v3 | B1 run | 后果 |
|---|---|---|---|---|
| 1 | `subset_policy` | `eghc_consensus_v1` | **`pathb_blk_arb_v1`** | 双变量，非单变量消融 |
| 2 | `route_guard_variant` | `eghc_consensus_v1` | `pathb_blk_arb_v1` | ← 这才是目标变量 |
| 3 | `mimo_retry_mode` | details 内显示为 `id_repair`（**待核 manifest**） | `none` | 若属实则系统性**低估** B1 |
| 4 | `git_commit` | `7a34492` | **`b782344`** | 代码基线不同，两 run 不可直接比 |
| 5 | `splits` | `["0"]`（ablation_matrix 内仅 split-0） | `["0","1","2"]` | full900 对比缺 EGHC3_v3 的 split1/2 逐样本数据 |
| 6 | `dirty_worktree` | — | **true** | 工作树脏，代码态不干净 |
| 7 | 统计 | 仅汇总比例 | 仅汇总比例 | **无配对、无 CI、无显著性检验** |

> ⇒ 现有"full900 +1.33pp"**不能**读作 B1 有效；它同时包含了 subset_policy、retry、git 基线三个混淆源的贡献。

---

## 3. 预注册（写死，禁事后改）

- **RDR 编号**：`RDR-20260921-PATHB-B1-ABLATION`（新建，待登记）
- **假设 H1**：在 EGHC3_v3 之上仅切换 `route_guard_variant=pathb_blk_arb_v1`，**full900 配对 ΔRSR > 0**。
- **判定规则（先写死）**：若 ΔRSR 的 95% CI **包含 0** → 判为**无显著提升**，直接 closure，**不为追分重跑、不改终点、不事后挑子集**。
- **负结果 = 有效 closure**（项目既定纪律）。

---

## 4. 实验设计

### 4.1 臂设计（严格单变量）

两臂**唯一差异 = `route_guard_variant`**，其余全部冻结：

| 配置项 | 臂 C（对照） | 臂 D（B1） |
|---|---|---|
| `model` | mimo-v2.5 | mimo-v2.5（同） |
| `subset_policy` | **`eghc_consensus_v1`** | **`eghc_consensus_v1`（必须同！）** |
| `route_guard_variant` | `eghc_consensus_v1` | **`pathb_blk_arb_v1`** ← 唯一变量 |
| `routing_variant` | `evidence_gated_cascade` | 同 |
| `segmentation_variant` | `eghc_mask_repair_v1` | 同 |
| `sam_backend` / `sam_model` | sam1 / vit_h / float32 | 同 |
| `prompt_variant` | legacy | 同 |
| `feature_source` | `deployable_predicted` | 同 |
| `max_output_tokens` | 1024 | 同 |
| `mimo_retry_mode` | **none** | **none**（两臂一致，消除混淆） |
| `molmo_cache_policy` / `cache_sha256` | reuse_existing / `705d088…` | **必须同一份缓存** |
| `splits` | 先 `["0"]`，后 `["0","1","2"]` | 同 |
| `git_commit` | 固定同一 HEAD（记录实际值） | 同 |

⚠️ **重要推论**：对照臂因此**不是**"原始 EGHC3_v3"（retry 由 `id_repair` 改为 `none`），而是"EGHC3_v3 的 retry=none 版本"。主终点是**两臂配对差**，**不是**与历史 62.00% 直接比。这一点必须在论文/报告中写明。

✅ **RDR 友好**：入口 `evaluate_mimo_full_protocol.py:63` 已支持 `--route-guard-variant`，跑 B1 臂**不需要改任何代码**，纯参数切换。

### 4.2 机械门 G0（跑之前必过）

1. 两臂 `run_manifest.json` **全键 diff**，断言唯一差异键 = `route_guard_variant`（+ 派生的 `guard_code_version`）。
2. 断言两臂 `cache_sha256` 相同。
3. 断言两臂 `git_commit` 相同、`dirty_worktree` 状态已记录。
4. 断言两臂样本 `(scene_id, object_id)` 集合**完全一致**（配对完整性）。

### 4.3 统计方法

- **配对**：以 `(scene_id, object_id)` 为键，逐样本取 `rsr_correct` / `ssr_correct`（字段已确认存在于 `details_*.json`）。
- **翻转表**：统计 B1 赢 / 基线赢 / 双成 / 双败 四格。
- **主检验**：**McNemar exact** 检验（配对二分类）。
- **效应量与 CI**：**配对 bootstrap，300 簇，seed 20260822**，输出 ΔRSR / ΔSSR 的 95% CI。
- **MDE（最小可检测效应）**：由 power 脚本在 Stage 0 精确给出。**纪律：阈值/概率类数字只采信脚本精确值，不手算、不报记忆值。**

### 4.4 机制分析（预注册为**探索性**，不得升格为主终点）

B1 只在 `blocker_first` 生效，因此：
- 用 `recommended_route` / `route_mode` / `frozen_route_used` / `route_guard_reasons`（字段已确认存在）筛出 **route == blocker_first** 的样本子集；
- 在该子集上重复 §4.3 的配对分析；
- 若全样本 Δ≈0 但 blocker_first 子集有效应 → 说明 B1 有效但被稀释，此时才值得为它做定向改造（新变体、新 RDR），**而不是**把它包装成主终点显著。

### 4.5 次要/过程指标

`repair_calls`、`calls_per_sample`（B1 n=900 为 71 / 2.07）、`route_guard_reasons` 分布、`pathb_keep` 触发率。

---

## 5. 执行阶段

| 阶段 | 内容 | 成本 | GPU | 产出 |
|---|---|---|---|---|
| **Stage 0** | 登记 RDR；跑 power 脚本定 MDE；`--help` 核对入口参数名；核对 EGHC3_v3 manifest 的 `mimo_retry_mode` | ¥0 | 否 | 预注册表 |
| **Stage A** | **离线再分析**：读两臂 `split_0/details_*.json`，配对 McNemar + bootstrap CI + blocker_first 条件分析 | **¥0** | **否** | ΔRSR/ΔSSR + CI + 翻转表 + 机制信号 |
| **Stage B** | 重跑**纯单变量臂 D**（先 split-0，视结果再 full900） | 付费（需授权） | **是（须先修驱动）** | 可比的两臂 manifest + details |
| **Stage C** | Stage A/B 结论合并 → 写入论文（若 null 则作为 limitation/负结果） | ¥0 | 否 | 结论 |

**推荐顺序：Stage 0 → A →（仅当 A 有 blocker_first 机制信号时）B → C。**

---

## 6. 前置阻塞（必须解除才能执行 Stage B）

> 来源：2026-09-21 00:00 服务器核查（10.99.16.20）

- `nvidia-smi` → **"couldn't communicate with the NVIDIA driver"**。
- 根因：9-20 17:00 `nvidia-dkms-580` 重装 dkms 编译失败（`dpkg error 1`），模块未进内核；且 `nvidia-dkms-580` 仍为 bionic 旧包 `580.126.09-0ubuntu0.18.04.1`，与 jammy 的 `libnvidia-* 580.178.04` 混装。
- 影响：pipeline 的 `sam_backend=sam1`（vit_h）等本地模型推理**跑不动**。
- 修复（待授权）：`apt install --reinstall nvidia-dkms-580 nvidia-driver-580`（`linux-headers-5.4.0-218-generic` 已装，满足 dkms 编译），验证 `nvidia-smi` 出卡。
- 附带：内核不一致（现运行 5.4.0-218，meta 指向 5.15.0-198）；`.bashrc` 仍 source 已卸载的 `/opt/ros/melodic/setup.bash`。

---

## 7. 红线

1. 不做事后挑子集当主终点；blocker_first 分析**预注册为探索性**。
2. Δ 的 CI 跨 0 即判 null，**不为追分重跑**。
3. 冻结仓 `/home/public/zb/FreeGrasp_code-freegrasp_codex` **只读**；新 run 落新目录。
4. 付费/重跑/改服务器须 boss **当轮授权**；预算用 `request_budget.py`（fail-closed，价格查官方出处，禁报记忆价）。
5. 交付文档中的命令**先 `--help` 核对参数名**再执行（历史坑：参数名不可凭记忆）。

---

## 8. 关键文件索引（服务器端）

| 用途 | 路径（相对 `/home/public/zb/FreeGrasp_code-freegrasp_codex`） |
|---|---|
| B1 变体定义 | `experiments/pathb_stage0_apply.py`（3-5 行）、`utils/eghc_router.py:20-25` |
| B1 仲裁代码 | `utils/eghc_adjudicator.py:325-345` |
| 已有 B1 结果 | `data/output/ablation_matrix/PATHB_FULL_B1_blk_arb/` |
| EGHC3_v3 基线 | `data/output/ablation_matrix/EGHC3_v3/`（仅 split_0） |
| guard→配置映射 | `experiment_manifest.py:80-88` |
| 评测入口 | `evaluate_mimo_full_protocol.py`（`--route-guard-variant` 在 63 行） |
| PathB 单测 | `experiments/pathb/test_pathb_stage0.py` |

---

## 9. Stage A 已执行（2026-09-21，¥0，结论：null closure）

完整结果见 **`PathB_B1_StageA_配对分析结果_20260921.md`**。要点：

- 主终点 ΔRSR = **+0.00 pp** [−3.00, +3.00]，p=1.0000；ΔSSR = **−0.33 pp** [−3.33, +2.67]，p=1.0000。
- 机制分层（基线臂 route 定义）：**blocker_first（n=150）ΔRSR −0.67 / ΔSSR −1.33 pp，CI 跨 0** ⇒ **无机制信号，不满足 Stage B 启动条件**。
- 路由迁移证据：blocker_first 150→141、target_first 18→25 ⇒ B1 行为确实生效，但**未转化为成功率增益**。
- ⇒ 按预注册规则判 null，**建议 closure，不启动 Stage B**。
