# EGHC3_v3（最高可部署方法）split-0 失败模式诊断

> 日期：2026-09-22 ｜ 只读、¥0、离线（数据已从服务器 `ablation_matrix` 只读 scp 到本地 `output/failure_mode_diag/`）
> 目的：判断「UNOGrasp 式感知/掩码改进」能否改善当前最高可部署方法的仿真失败。诊断脚本 `failure_mode_diag.py`。

## 0. 一句话结论

**EGHC3_v3 split-0 的失败几乎 100% 是「选错目标物体」（推理/选择层），不是「掩码质量差」（感知层）。** 即便失败 case 的 mask IoU 高达 0.98，只要目标选错就判失败。因此 UNOGrasp 式的感知/掩码改进**基本打不到当前主导失败模式**。

> ⚠️ 这与此前（基于 R/M 校准数据集）「瓶颈可能在感知层」的假设相反。注意：R/M 校准集与 EGHC3_v3 是不同数据集，本诊断只针对**实际要部署的 EGHC3_v3**，不直接推翻那条假设，但给出了更强的反证据。

## 1. 总体数据（split-0, n=300）

| 指标 | 值 |
|---|---|
| 总 case | 300 |
| RSR 成功 (rsr=1) | 186 (62.0%) |
| RSR 失败 (rsr=0) | 114 (38.0%) |

## 2. 失败分类（核心发现）

用两个信号交叉判定失败原因：
- **目标选择正确性**：`selected_gt_id ∈ gt_ids` ？（`gt_ids`=场景真实可抓目标集合）
- **pipeline 自身标签**：顶层 `failure_reason`

| 失败子集 | 数量 | 占失败比 |
|---|---|---|
| 选错目标（selected_gt_id ∉ gt_ids） | **114** | **100%** |
| 目标选对但失败（selected ∈ gt_ids） | 0 | 0% |
| └ 其中掩码/感知失败 | 0 | 0% |
| └ 其中姿态/仿真失败 | 0 | 0% |

**验证分类器有效性**：成功 case 中 `selected_gt_id ∈ gt_ids` = 186/186 (100%)；失败 case = 0/114。两个集合被「目标选择正确性」**完美分离** → 对 EGHC3_v3 split-0 而言，成败 ≈ 「目标选对没」。

### 2.1 failure_reason 分布（pipeline 标签，仅供参考）

| failure_reason | 数量 | 说明 |
|---|---|---|
| `iou_below_threshold` | 106 | **误导性标签**：IoU 是对「正确 gt」算的，选错物体自然=0，并非掩码差 |
| `None` | 8 | ssr=1 但 rsr=0；全部同样 selected ∉ gt_ids（目标错），属特殊子型 |

### 2.2 反例证明掩码不是瓶颈

| scene | 选中 | 正确 gt | IoU | 解读 |
|---|---|---|---|---|
| 5636 | obj 11 | obj 2 | **0.984** | mask 极好，但选错物体 → 失败 |
| 2276 | obj 3 | obj 2 | 0.977 | 同上 |
| 6751 | obj 3 | obj 8 | 0.952 | 同上 |
| 1709 | obj -1 | obj 4,12 | 0.954 | 解析到无效 id |

→ **掩码质量高也不救失败**；决定因素是被选中的物体 id 是否正确。

## 3. 失败与场景属性

### 3.1 目标可见度（target_visibility）

| 可见度 | 失败 | 成功 | 失败率 |
|---|---|---|---|
| visible_but_blocked（可见但被遮挡） | **78** | 113 | 40.8% |
| free_visible（完全可见） | 35 | 71 | 33.0% |
| mostly_hidden（基本被遮） | 1 | 2 | 33.3% |

→ 失败**集中在「可见但被遮挡」场景**（78 例，占失败 68%），而非「基本被遮」这种最难的。说明问题出在**有遮挡/关系时模型认错目标**，不是看不见。

### 3.2 路由（route_mode，失败 case 内）

| 路由 | 失败数 |
|---|---|
| blocker_first（已识别遮挡并启用硬路由） | 60 |
| direct | 40 |
| target_first | 14 |

→ 60/114 失败 case 已正确进入 `blocker_first` 路由（系统**检测到了遮挡**），但仍选错目标。这是**关系推理/目标识别**失败，不是遮挡没检测到。

## 4. PATHB B1 对照（机制定位）

| arm | 失败数 | 选错目标 | 目标对但失败 |
|---|---|---|---|
| EGHC3_v3 | 114 | 114 | 0 |
| PATHB_FULL_B1_blk_arb | 114 | 114 | 0 |

B1 仅改变了路由分布（blocker_first 60→55、direct 40→45），对**目标选择正确性零影响**。这双重确认：
1. Stage A 的 null 结论（ΔRSR 0.00pp）；
2. 失败发生在比路由更上游的**目标选择层**，B1（路由仲裁微调）碰不到 → 不应再投入 B1 相关消融。

## 5. 对 UNOGrasp 可迁移性的判定

UNOGrasp 的核心贡献是**感知/掩码向**的：视觉脚手架（ratio/contact/degree/SoM）、遮挡图关系先验（ancestors/blockers）、SFT→RFT。这些改善的是**掩码质量与关系先验**。

基于本诊断：
- EGHC3_v3 的失败 **100% 是目标选择/推理错误，0% 是掩码质量**。→ UNOGrasp 式改进**打不到主导失败模式**。
- 唯一可能沾边的是遮挡图关系先验（对 blocker_first 那 60 例），但那批失败是**目标 id 认错**、不是遮挡没检测到 → 顶多边际收益。
- **结论：对 EGHC3_v3，不应把精力放在 UNOGrasp 式感知改进上；瓶颈在目标识别/选择推理层。** 若有收益空间，应在「目标识别/关系推理」（如更强的路由器、目标 id 对齐、query 与场景图对齐）而非掩码。

## 6. 限制与下一步

- **范围**：仅 split-0 (n=300)，非 full900；仅 EGHC3_v3（最高可部署）。更低性能方法 / full900 的失败构成可能不同，需复核。
- **启发式**：`selected_gt_id ∉ gt_ids` 作为「选错目标」代理，已用成功集完美分离验证，可靠；但无法解释**模型为何选错**（需对路由器原始输出做定性错误分析）。
- **建议下一步**（按性价比）：
  1. （¥0）定性抽查 10–20 个选错目标的 case 的 `router_raw_output` / `target_candidates`，定位是「语言歧义」「遮挡混淆」还是「id 错位」。
  2. （¥0）对 full900 跑同样诊断，确认结论是否跨规模成立。
  3. 若确认瓶颈在目标识别，评估「目标识别/关系推理」方向的改进（非 UNOGrasp 感知方向）。
  4. UNOGrasp 的遮挡图先验可作为**次要**探索项，但预期收益低，不应作为主线。

## 7. 数据与方法出处

- 输入：`/home/public/zb/FreeGrasp_code-freegrasp_codex/data/output/ablation_matrix/{EGHC3_v3,PATHB_FULL_B1_blk_arb}/split_0/details_*.json`（2026-09-22 只读 scp 至本地 `E:/FreeGrasp_code-main/output/failure_mode_diag/`）
- 脚本：`E:/FreeGrasp_code-main/failure_mode_diag.py`（纯本地、无 API/GPU）
- 关联结论：Stage A 配对分析（`PathB_B1_StageA_配对分析结果_20260921.md`）→ ΔRSR +0.00pp，本报告给出其机制解释。
